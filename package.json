{
  "name": "tikhub",
  "version": "1.0.0",
  "description": "OpenClaw plugin for TikHub social media data API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "openclaw": {
    "extensions": ["dist/index.js"]
  }
}
